#include <stdio.h>
int main() {
    int n;
    scanf("%d", &n);
    int a[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    int s;
    scanf("%d", &s);
    int sum=0;
    for (int k = 1; k < n; k++) {

        for (int j=0;j< n;j++) {

            if (a[j] == s) {
                
            }
        }

    }
}




